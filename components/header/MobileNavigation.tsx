type Props = {
  pages: {
    name: string;
    href: string;
  }[];
  className: string;
};

export default function MobileNavigation({
  pages,
  className,
}: Props): JSX.Element {
  return <></>;
}
