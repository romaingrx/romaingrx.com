---
import type { ImageMetadata } from 'astro';
import ImageViewer from './image-viewer.astro';

interface Props {
  src: ImageMetadata;
}

const { src, ...props } = Astro.props;
---

<ImageViewer src={src} {...props} />
