---
import { ArrowUpRight } from 'lucide-react';
import { Card, CardDescription, CardTitle } from '@/components/ui/react/card';
import { Link } from '@/components/ui/react/link';
import { cn } from '@/lib/utils';

export interface Props {
  title: string;
  value: number;
  description?: string;
  link?: string;
  class?: string;
}

const { title, value, description, link, class: className } = Astro.props;
---

<Card className={cn('flex flex-row items-center justify-between gap-4 px-4 py-3', className)}>
  <div class="flex min-w-0 flex-col gap-0.5">
    <CardTitle className="text-sm font-medium">{title}</CardTitle>
    {description && <CardDescription className="text-xs">{description}</CardDescription>}
  </div>
  <div class="flex shrink-0 items-center gap-2">
    <span class="text-2xl font-bold tabular-nums">{value.toLocaleString()}</span>
    {
      link && (
        <Link
          href={link}
          target="_blank"
          rel="noopener noreferrer"
          variant="secondary"
          size="icon"
          className="group"
          aria-label={`Open ${title}`}
        >
          <ArrowUpRight className="transition-all duration-300 group-hover:-translate-y-1 group-hover:translate-x-1" />
        </Link>
      )
    }
  </div>
</Card>
