---
import '@/styles/typography.css';
import { cn } from '@/lib/utils';

export interface Props {
  Element?: 'article' | 'div';
  class?: string;
}

const { Element = 'div', class: className } = Astro.props;
---

<Element class={cn('prose', className)}>
  <slot />
</Element>
