---
import { fieldVariants } from './variants';
import { cn } from '@/lib/utils';

type Props = astroHTML.JSX.InputHTMLAttributes & {
  className?: string;
};

const { className, class: cls, ...props } = Astro.props;
---

<input {...props} data-slot="input" class={cn(fieldVariants(), className, cls)} />
