---
import { cn } from '@/lib/utils';

type Props = astroHTML.JSX.TextareaHTMLAttributes & {
  className?: string;
};

const { className, class: cls, ...props } = Astro.props;
---

<textarea class={cn('input', className, cls)} {...props}><slot /></textarea>
