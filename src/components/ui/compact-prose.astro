---
import '@/styles/typography.css';
import { cn } from '@/lib/utils';

export interface Props {
  Element?: 'div' | 'section' | 'article';
  class?: string;
}

const { Element = 'div', class: className } = Astro.props;
---

<Element class={cn('prose-compact', className)}>
  <slot />
</Element>
